use "other.lp";

const var = 20;

fn wowie(number num, number some) {
	printf("wowie. num: %d : some: %d", num, some); 
	return;
}

fn main() {
	printf("hello");
	wowie(200, 300);
	lol(100);
	return;
}
