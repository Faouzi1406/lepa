use "other.lp";

const var = 20;

fn wow(number var) number {
	printf("Isn't this so strange...! number: %s %d %s -- ", "this is sutpid!!", var, "\n"); 
	lol();
	return 1;
}

fn main() {
	let other = wow(2);
	return;
}
